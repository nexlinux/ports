name: django-cors-headers
version: 4.9.0
source:
  url: https://github.com/adamchainz/django-cors-headers/archive/4.9.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - asgiref
  - django
  runtime:
  - asgiref
  - django
