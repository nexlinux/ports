name: elfeed
version: 4.1.1
source:
  url: https://github.com/emacs-elfeed/elfeed/archive/4.1.1.tar.gz
build:
- make -j$(nproc)
- make DESTDIR=$DESTDIR PREFIX=$PREFIX install
depends:
  build:
  - curl
  - compat
  runtime:
  - curl
  - compat
