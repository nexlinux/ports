name: sybil
version: 10.1.0
source:
  url: https://github.com/simplistix/sybil/archive/10.1.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - myst-parser
  - seedir
  - testfixtures
  - pytest
  runtime:
  - pytest
