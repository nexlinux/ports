name: websocket-client
version: 1.9.0
source:
  url: https://github.com/websocket-client/websocket-client/archive/refs/tags/v1.9.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - python-socks
