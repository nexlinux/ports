name: langkit-contrib
version: 26.0.0
source:
  url: https://github.com/AdaCore/langkit/archive/refs/tags/v26.0.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - e3-core
  - gprbuild
  - langkit
  runtime:
  - langkit
