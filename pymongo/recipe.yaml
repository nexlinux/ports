name: pymongo
version: 4.17.0
source:
  url: https://github.com/mongodb/mongo-python-driver/archive/4.17.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools
  - mongodb
  - dnspython
  - kerberos
  runtime:
  - dnspython
  - kerberos
