name: threadpoolctl
version: 3.6.0
source:
  url: https://github.com/joblib/threadpoolctl/archive/3.6.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - cython
  - numpy
  - scipy
  - setuptools
