name: spotipy
version: 2.26.0
source:
  url: https://github.com/spotipy-dev/spotipy/archive/2.26.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - redis
  - requests
  - urllib3
  runtime:
  - redis
  - requests
  - urllib3
