name: requests-ntlm
version: 1.3.0
source:
  url: https://github.com/requests/requests-ntlm/archive/v1.3.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - flask
  - cryptography
  - pyspnego
  - requests
  runtime:
  - cryptography
  - pyspnego
  - requests
