name: docker
version: 7.2.0
source:
  url: https://github.com/docker/docker-py/archive/7.2.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - hatch-vcs
  - paramiko
  - websocket-client
  - requests
  - urllib3
  runtime:
  - requests
  - urllib3
