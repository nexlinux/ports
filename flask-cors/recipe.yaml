name: flask-cors
version: 6.0.4
source:
  url: https://github.com/corydolphin/flask-cors/archive/6.0.4.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - packaging
  - flask
  - werkzeug
  runtime:
  - flask
  - werkzeug
