name: sphinx-notfound-page
version: 1.1.0
source:
  url: https://github.com/readthedocs/sphinx-notfound-page/archive/1.1.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - sphinx
  runtime:
  - sphinx
