name: cli-helpers
version: 2.15.0
source:
  url: https://github.com/dbcli/cli_helpers/archive/v2.15.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - configobj
  - pygments
  - tabulate
  - wcwidth
  runtime:
  - configobj
  - pygments
  - tabulate
  - wcwidth
