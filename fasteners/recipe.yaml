name: fasteners
version: '0.20'
source:
  url: https://github.com/harlowja/fasteners/archive/0.20.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - diskcache
  - more-itertools
