name: flake8
version: 7.3.0
source:
  url: https://github.com/PyCQA/flake8/archive/7.3.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pyflakes
  - pycodestyle
  runtime:
  - pyflakes
  - pycodestyle
