name: cloudpickle
version: 3.1.2
source:
  url: https://github.com/cloudpipe/cloudpickle/archive/v3.1.2.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - flit-core
  - psutil
