name: unifont
version: 17.0.05
source:
  url: https://ftp.gnu.org/gnu/unifont/unifont-17.0.05/unifont-17.0.05.tar.gz
build:
- make -j$(nproc)
- make DESTDIR=$DESTDIR PREFIX=$PREFIX install
depends:
  build:
  - fontconfig
  - bdf2psf
  - bdftopcf
  - fontforge
