name: google-i18n-address
version: 3.1.1
source:
  url: https://github.com/mirumee/google-i18n-address/archive/3.1.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - requests
  runtime:
  - requests
