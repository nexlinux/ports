name: fitsio
version: 1.4.0
source:
  url: https://github.com/esheldon/fitsio/archive/1.4.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - numpy
  - cfitsio
  runtime:
  - setuptools-scm
