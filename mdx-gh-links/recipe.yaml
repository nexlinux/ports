name: mdx-gh-links
version: '0.4'
source:
  url: https://github.com/Python-Markdown/github-links/archive/0.4.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - markdown-3
  runtime:
  - markdown-3
