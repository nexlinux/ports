name: rpl
version: 1.15.5
source:
  url: https://github.com/rrthomas/rpl/archive/v1.15.5.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - argparse-manpage
  - regex
  - chainstream
  - pytest-datafiles
  - chardet
  runtime:
  - chardet
