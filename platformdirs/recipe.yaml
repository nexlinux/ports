name: platformdirs
version: 4.11.3
source:
  url: https://github.com/platformdirs/platformdirs/archive/refs/tags/4.11.3.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - appdirs
