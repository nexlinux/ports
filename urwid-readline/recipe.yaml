name: urwid-readline
version: 0.15.1
source:
  url: https://github.com/rr-/urwid_readline/archive/0.15.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - urwid
  runtime:
  - urwid
