name: graphviz
version: '0.21'
source:
  url: https://github.com/xflr6/graphviz/archive/0.21.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - mock
  - graphviz
  runtime:
  - graphviz
