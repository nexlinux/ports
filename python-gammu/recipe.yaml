name: python-gammu
version: 3.4.1
source:
  url: https://github.com/gammu/python-gammu/archive/3.4.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pkgconfig
  - gammu
