name: tifffile
version: 2026.8.23
source:
  url: https://github.com/cgohlke/tifffile/archive/v2026.8.23.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - defusedxml
  - fsspec
  - lxml
  - xarray
  - numpy
  runtime:
  - numpy
