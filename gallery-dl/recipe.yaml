name: gallery-dl
version: 1.32.10
source:
  url: https://github.com/mikf/gallery-dl/archive/v1.32.10.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - requests
  runtime:
  - requests
