name: loky
version: 3.6.0
source:
  url: https://github.com/joblib/loky/archive/3.6.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - numpy
  - packaging
  - cloudpickle
  - psutil
  runtime:
  - cloudpickle
  - psutil
