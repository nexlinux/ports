name: wcmatch
version: '11.0'
source:
  url: https://github.com/facelessuser/wcmatch/archive/11.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - git
  - bracex
  runtime:
  - bracex
