name: requests-toolbelt
version: 1.0.0
source:
  url: https://github.com/requests/toolbelt/archive/1.0.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - betamax
  - cryptography
  - pyopenssl
  - trustme
  - requests-3
  runtime:
  - requests-3
