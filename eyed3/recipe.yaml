name: eyed3
version: 0.9.9
source:
  url: https://github.com/nicfit/eyeD3/archive/v0.9.9.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - packaging
  - pillow
  - pylast
  - deprecation
  - filetype
  - pyyaml
  runtime:
  - deprecation
  - filetype
  - pyyaml
