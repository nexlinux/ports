name: pytest-reraise
version: 2.1.2
source:
  url: https://github.com/bjoluc/pytest-reraise/archive/v2.1.2.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pytest
  runtime:
  - pytest
