name: pysnmp
version: 7.1.29
source:
  url: https://github.com/lextudio/pysnmp/archive/v7.1.29.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - cryptography
  - pyasn1
  - pysmi
  runtime:
  - cryptography
  - pyasn1
  - pysmi
