name: pyenchant
version: 3.3.0
source:
  url: https://github.com/pyenchant/pyenchant/archive/v3.3.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - myspell-en
  - enchant
  runtime:
  - enchant
