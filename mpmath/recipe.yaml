name: mpmath
version: 1.4.1
source:
  url: https://github.com/mpmath/mpmath/archive/refs/tags/1.4.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
  - numpy
  - packaging
  - pexpect
  - gmpy2
  - matplotlib
