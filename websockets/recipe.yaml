name: websockets
version: 17.0.1
source:
  url: https://github.com/python-websockets/websockets/archive/17.0.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - trio
  - werkzeug
