name: prettytable
version: 3.17.0
source:
  url: https://github.com/prettytable/prettytable/archive/refs/tags/3.17.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - hatch-vcs
  - wcwidth
  runtime:
  - wcwidth
