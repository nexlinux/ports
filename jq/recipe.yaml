name: jq
version: 1.8.0
source:
  url: https://github.com/mwilliamson/jq.py/archive/1.8.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - cython
  - jq
  - oniguruma
