name: rapidfuzz-cpp
version: 3.3.3
source:
  url: https://github.com/rapidfuzz/rapidfuzz-cpp/archive/v3.3.3.tar.gz
build:
- cmake -B build -DCMAKE_INSTALL_PREFIX=$PREFIX
- cmake --build build -j$(nproc)
- DESTDIR=$DESTDIR cmake --install build
depends:
  build:
  - catch
