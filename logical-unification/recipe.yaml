name: logical-unification
version: 0.4.7
source:
  url: https://github.com/pythological/unification/archive/v0.4.7.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
  - multipledispatch
  - toolz
  runtime:
  - multipledispatch
  - toolz
