name: httplib2
version: 0.31.1
source:
  url: https://github.com/httplib2/httplib2/archive/v0.31.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - openssl
  - cryptography
  - pysocks
  - certifi
  - pyparsing
  runtime:
  - certifi
  - pyparsing
  - pysocks
