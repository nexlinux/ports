name: gmpy2
version: 2.3.1
source:
  url: https://github.com/gmpy2/gmpy2/archive/refs/tags/v2.3.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
  - mpmath
  - numpy
  - mpc
  - mpfr
  - gmp
