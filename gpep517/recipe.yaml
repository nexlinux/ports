name: gpep517
version: '22'
source:
  url: https://github.com/gentoo/gpep517/archive/v22.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - installer
  runtime:
  - installer
