name: cons
version: 0.4.7
source:
  url: https://github.com/pythological/python-cons/archive/v0.4.7.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
  - logical-unification
  runtime:
  - logical-unification
