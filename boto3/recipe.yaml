name: boto3
version: 1.43.36
source:
  url: https://github.com/boto/boto3/archive/1.43.36.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - botocore-
  - jmespath-0
  - s3transfer-0
  runtime:
  - botocore-
  - jmespath-0
  - s3transfer-0
