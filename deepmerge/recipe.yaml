name: deepmerge
version: 2.1.0
source:
  url: https://github.com/toumorokoshi/deepmerge/archive/v2.1.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
