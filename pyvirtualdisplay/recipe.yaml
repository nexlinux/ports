name: pyvirtualdisplay
version: '3.0'
source:
  url: https://github.com/ponty/PyVirtualDisplay/archive/3.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - easyprocess
  - entrypoint2
  - path
  - pillow
  - psutil
  - pyscreenshot
  - xmessage
  - xorg-server
