name: warlock
version: 2.1.0
source:
  url: https://github.com/bcwaldon/warlock/archive/2.1.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - jsonpatch
  - jsonschema
  runtime:
  - jsonpatch
  - jsonschema
