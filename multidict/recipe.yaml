name: multidict
version: 6.7.1
source:
  url: https://github.com/aio-libs/multidict/archive/v6.7.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - objgraph
  - psutil
