name: pythonnet
version: 3.1.0
source:
  url: https://github.com/pythonnet/pythonnet/archive/refs/tags/v3.1.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - psutil
  - clr-loader
  - pycparser
  runtime:
  - clr-loader
  - pycparser
