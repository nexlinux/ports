name: markdown-it-py
version: 4.2.0
source:
  url: https://github.com/executablebooks/markdown-it-py/archive/v4.2.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - requests
  - linkify-it-py
  - mdurl
  runtime:
  - linkify-it-py
  - mdurl
