name: aiohttp-cors
version: 0.8.1
source:
  url: https://github.com/aio-libs/aiohttp-cors/archive/v0.8.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - selenium
  - aiohttp
  runtime:
  - aiohttp
