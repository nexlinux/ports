name: bnf-mode
version: 0.4.5
source:
  url: https://github.com/sergeyklay/bnf-mode/archive/0.4.5.tar.gz
build:
- make -j$(nproc)
- make DESTDIR=$DESTDIR PREFIX=$PREFIX install
depends:
  build:
  - undercover
