name: python-multipart
version: 0.0.32
source:
  url: https://github.com/Kludex/python-multipart/archive/0.0.32.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pyyaml
