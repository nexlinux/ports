name: p1_oauth2
version: 0.6.14
source:
  url: https://github.com/processone/p1_oauth2/archive/0.6.14.tar.gz
build:
- make -j$(nproc)
- make DESTDIR=$DESTDIR PREFIX=$PREFIX install
depends:
  build:
  - erlang
