name: time-machine
version: 3.4.0
source:
  url: https://github.com/adamchainz/time-machine/archive/3.4.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - freezegun
  - python-dateutil
  - tokenize-rt
