name: imageio-ffmpeg
version: 0.6.0
source:
  url: https://github.com/imageio/imageio-ffmpeg/archive/v0.6.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - ffmpeg
  runtime:
  - ffmpeg
