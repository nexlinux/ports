name: pypdf
version: 6.16.1
source:
  url: https://github.com/py-pdf/pypdf/archive/6.16.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - cryptography
  - fonttools
  - pillow
  - pyyaml
  - jbig2dec
