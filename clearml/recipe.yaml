name: clearml
version: 1.18.0
source:
  url: https://github.com/clearml/clearml/archive/refs/tags/v1.18.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pyjwt
  - furl
