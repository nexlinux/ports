name: lwt_log
version: 1.1.2
source:
  url: https://github.com/ocsigen/lwt_log/archive/1.1.2.tar.gz
build:
- make -j$(nproc)
- make DESTDIR=$DESTDIR PREFIX=$PREFIX install
depends:
  build:
  - lwt
  runtime:
  - lwt
