name: bibtexparser
version: 1.4.4
source:
  url: https://github.com/sciunto-org/python-bibtexparser/archive/v1.4.4.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pyparsing
  runtime:
  - pyparsing
