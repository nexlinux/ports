name: certifi
version: 3024.7.22
source:
  url: https://github.com/projg2/certifi-system-store/archive/v3024.7.22.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - ca-certificates
  runtime:
  - ca-certificates
