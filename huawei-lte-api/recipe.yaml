name: huawei-lte-api
version: 1.11.0
source:
  url: https://github.com/Salamek/huawei-lte-api/archive/1.11.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pycryptodome
  - requests
  - xmltodict
  runtime:
  - pycryptodome
  - requests
  - xmltodict
