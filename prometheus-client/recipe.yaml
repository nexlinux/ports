name: prometheus-client
version: 0.25.0
source:
  url: https://github.com/prometheus/client_python/archive/v0.25.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - twisted
  runtime:
  - twisted
