name: hatch-vcs
version: 0.5.0
source:
  url: https://github.com/ofek/hatch-vcs/archive/v0.5.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - git
  - setuptools-scm
  runtime:
  - setuptools-scm
