name: construct
version: 2.10.70
source:
  url: https://github.com/construct/construct/archive/v2.10.70.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - arrow
  - cloudpickle
  - cryptography
  - lz4
  - numpy
  - ruamel-yaml
