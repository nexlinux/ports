name: djangorestframework
version: 3.17.1
source:
  url: https://github.com/encode/django-rest-framework/archive/3.17.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - inflection
  - pyyaml
  - pytz
  - django
  runtime:
  - django
