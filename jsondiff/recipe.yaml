name: jsondiff
version: 2.2.1
source:
  url: https://github.com/xlwings/jsondiff/archive/2.2.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
  - pyyaml
  runtime:
  - pyyaml
