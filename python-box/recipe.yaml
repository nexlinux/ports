name: python-box
version: 7.4.1
source:
  url: https://github.com/cdgriffith/Box/archive/7.4.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - cython
  - msgpack
  - ruamel-yaml
  - tomli-w
  runtime:
  - msgpack
  - ruamel-yaml
  - tomli-w
