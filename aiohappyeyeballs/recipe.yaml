name: aiohappyeyeballs
version: 2.6.2
source:
  url: https://github.com/aio-libs/aiohappyeyeballs/archive/v2.6.2.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - poetry-core
