name: agate-dbf
version: 0.2.4
source:
  url: https://github.com/wireservice/agate-dbf/archive/0.2.4.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - agate-1
  - dbfread-2
  runtime:
  - agate-1
  - dbfread-2
