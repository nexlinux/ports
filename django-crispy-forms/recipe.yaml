name: django-crispy-forms
version: '2.7'
source:
  url: https://github.com/django-crispy-forms/django-crispy-forms/archive/2.7.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - crispy-bootstrap3
  - crispy-bootstrap4
  - sh
  - django
  runtime:
  - django
