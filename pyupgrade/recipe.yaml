name: pyupgrade
version: 3.21.2
source:
  url: https://github.com/asottile/pyupgrade/archive/v3.21.2.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - tokenize-rt-6
  runtime:
  - tokenize-rt-6
