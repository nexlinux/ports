name: portalocker
version: 4.0.0
source:
  url: https://github.com/WoLpH/portalocker/archive/v4.0.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - fakeredis
  - redis
  - sphinx
