name: frozenlist
version: 1.8.0
source:
  url: https://github.com/aio-libs/frozenlist/archive/v1.8.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - expandvars
  - setuptools
  - wheel
  - cython
