name: yadm
version: 3.5.0
source:
  url: https://github.com/yadm-dev/yadm/archive/3.5.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pytest
  - expect
  - gnupg
  - bash
  - git
  runtime:
  - gnupg
  - bash
  - git
