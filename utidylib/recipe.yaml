name: utidylib
version: 1.0.0
source:
  url: https://github.com/nijel/utidylib/archive/v1.0.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - htmltidy
  runtime:
  - htmltidy
