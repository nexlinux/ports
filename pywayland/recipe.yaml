name: pywayland
version: 0.4.19
source:
  url: https://github.com/flacjacket/pywayland/archive/refs/tags/v0.4.19.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools
  - wayland-scanner
  - wayland
  - cffi
  runtime:
  - wayland
  - cffi
