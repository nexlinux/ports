name: fisicalab
version: 0.4.0
source:
  url: https://ftp.gnu.org/gnu/fisicalab/fisicalab-0.4.0.tar.gz
build:
- ./configure --prefix=$PREFIX
- make -j$(nproc)
- make DESTDIR=$DESTDIR install
depends:
  build:
  - renaissance
  - gsl
  - gnustep-back
