name: joblib
version: 1.5.3
source:
  url: https://github.com/joblib/joblib/archive/refs/tags/1.5.3.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - threadpoolctl
  - cloudpickle
  - loky
  runtime:
  - cloudpickle
  - loky
