name: jc
version: 1.25.7
source:
  url: https://github.com/kellyjonbrazil/jc/archive/v1.25.7.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pygments
  - ruamel-yaml
  - xmltodict
  runtime:
  - pygments
  - ruamel-yaml
  - xmltodict
