name: botocore
version: 1.43.51
source:
  url: https://github.com/boto/botocore/archive/1.43.51.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - jsonschema
  - jmespath
  - python-dateutil
  - urllib3
  runtime:
  - jmespath
  - python-dateutil
  - urllib3
