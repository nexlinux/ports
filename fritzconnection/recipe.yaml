name: fritzconnection
version: 1.15.1
source:
  url: https://github.com/kbr/fritzconnection/archive/1.15.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - requests
  - segno
  runtime:
  - requests
  - segno
