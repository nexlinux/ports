name: watchfiles
version: 1.2.0
source:
  url: https://github.com/samuelcolvin/watchfiles/archive/v1.2.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-rust
  - dirty-equals
  - anyio-4
  runtime:
  - anyio-4
