name: gitchangelog
version: 3.2.0
source:
  url: https://github.com/sarnold/gitchangelog/releases/download/3.2.0/gitchangelog-3.2.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - minimock
  - pystache
  - mako
  runtime:
  - pystache
  - mako
