name: python-memcached
version: '1.62'
source:
  url: https://github.com/linsomniac/python-memcached/archive/1.62.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - memcached
