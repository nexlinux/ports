name: erlang-mode
version: 27.0.1
source:
  url: https://github.com/erlang/otp/archive/OTP-27.0.1.tar.gz
build:
- ./configure --prefix=$PREFIX
- make -j$(nproc)
- make DESTDIR=$DESTDIR install
depends:
  build:
  - erlang
  runtime:
  - erlang
