name: pytest-django
version: 4.13.0
source:
  url: https://github.com/pytest-dev/pytest-django/archive/v4.13.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - setuptools-scm
  - django
  - django-configurations
  - pytest
  runtime:
  - pytest
