name: sphinx-autoapi
version: 3.8.0
source:
  url: https://github.com/readthedocs/sphinx-autoapi/archive/v3.8.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - astroid
  - jinja2
  - pyyaml
  - sphinx
  runtime:
  - astroid
  - jinja2
  - pyyaml
  - sphinx
