name: lttng-ust
version: 2.14.2
source:
  url: https://lttng.org/files/lttng-ust/lttng-ust-2.14.2.tar.bz2
build:
- ./configure --prefix=$PREFIX
- make -j$(nproc)
- make DESTDIR=$DESTDIR install
depends:
  build:
  - pkgconfig
  - perl
  - openpgp-keys-mathieudesnoyers
  - userspace-rcu
  - numactl
