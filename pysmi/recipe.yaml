name: pysmi
version: 2.0.0
source:
  url: https://github.com/lextudio/pysmi/archive/v2.0.0.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - pysnmp
  - jinja2
  - lark
  - requests
  runtime:
  - jinja2
  - lark
  - requests
