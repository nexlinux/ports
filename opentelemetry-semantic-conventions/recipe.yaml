name: opentelemetry-semantic-conventions
version: 1.42.1
source:
  url: https://github.com/open-telemetry/opentelemetry-python/archive/refs/tags/v1.42.1.tar.gz
build:
- pip install . --root=$DESTDIR --prefix=$PREFIX
depends:
  build:
  - typing-extensions
  - opentelemetry-api-
  runtime:
  - opentelemetry-api-
  - typing-extensions
